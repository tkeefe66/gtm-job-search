import { readFileSync, writeFileSync } from 'node:fs';
const fonts = Object.fromEntries(['Sans','Serif'].map(name=>[name.toLowerCase(),readFileSync(`assets/resume-builder/v1/Noto${name}.ttf`).toString('base64')]));
writeFileSync('lib/resume-builder-font-assets.ts','// Generated by scripts/build-resume-font-assets.mjs. OFL licenses in assets/resume-builder/v1.\nexport const BUILDER_FONTS = '+JSON.stringify(fonts)+' as const;\n');
